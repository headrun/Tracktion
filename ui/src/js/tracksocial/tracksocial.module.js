;(function (angular) {
  "use strict";

  angular.module("tracksocial", ["ui.router", "filterBar", "highcharts",
                                "layout", "utils", "tab", "ngJustGage", "usMap" ]);
}(window.angular));
